{% extends '_partials/base_layout' %}

{% block body %}
    <div class="flex flex-col min-h-screen">
        {% include '_partials/components/header_image' %}

        <div class="flex-grow">
            {% block content %}{% endblock %}
        </div>

        <div class="bg-gray-800 text-white mt-12">
            <div class="h-12 bg-gray-200"></div>
            <div class="text-center pt-12 pb-16">Copyright 2017–{{ date() | date('Y') }} Naomi Bennett</div>
        </div>
    </div>
{% endblock %}